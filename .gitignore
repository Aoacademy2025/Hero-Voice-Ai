# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual envs
venv/
venv_*/
.venv/

# Model weights (โหลดแยก — ใหญ่เกินจะ commit)
model/
model_dl/

# Runtime data
logs/
*.log
credits.db
credits.db-*
saved_outputs/
custom_voices/
test_outputs/

# Audio artifacts (เก็บเฉพาะ voices/ ที่ build ไว้)
/*.wav
out.wav

# OS / editor
.DS_Store
Thumbs.db

# Packaging artifacts
*.zip
test_handler_*.wav
__pycache__/

# LLM transliteration cache (runtime-generated, machine-specific)
translit_cache.json

# A/B test scratch output + manifest backups (regenerated locally, not source)
ab_output/
voices/*.bak-*
out.txt
*.mp4

# เครื่องมือรันเฉพาะเครื่อง — มักฝัง API key จริง (เช่น start-heroai.ps1) ห้าม commit
start-*.ps1
*.local.ps1
